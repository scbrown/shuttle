"""shuttle — the workflow engine of the quipu stack.

Carries runs through their steps and exports signed, append-only transition
history into quipu's windowed operational graphs. See docs/design/shuttle.md.
"""

__version__ = "0.2.0"
